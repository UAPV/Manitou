generator:
  class: sfPropelGenerator
  param:
    model_class:           Image
    theme:                 admin15
    non_verbose_templates: true
    with_show:             false
    singular:              Image
    plural:                Images
    route_prefix:          image
    with_propel_route:     1
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  ~
      list:
        title: Images disponibles
        display: [filename, comment, host, image_server]
      filter:  ~
      form:    ~
      edit:    ~
      new:
        actions:
          save: { label: Créer }
